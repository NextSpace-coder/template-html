{
  "name": "html-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run dev:bs",
    "build": "rimraf dist && cpx \"pages/**/*\" dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:bs": "browser-sync start --config bs.config.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "cpx": "^1.5.0",
    "rimraf": "^6.0.1"
  },
  "pnpm": {
    "overrides": {
      "anymatch": "^3.1.3"
    }
  }
}
